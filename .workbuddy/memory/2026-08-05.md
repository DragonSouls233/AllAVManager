# 2026-08-05 工作日志

## 数据库架构全面重建（v2.0）

### 背景
分析 `MDCX数据流向总结.md` 发现大量架构冲突：
- scraper.db 声称"废弃"但收藏夹/头像导入仍依赖
- 头像API路径不一致（/api/v1/actors/ vs /api/v1/jav/actors/）
- 新库架构退化（逗号分隔演员 vs 多对多）
- Gfriends路径全部指向不存在位置
- output_dir 优先级1对JAV模块永远不生效

### 完成工作
彻底重建数据库架构，从单体+模块混合 → 完全独立的7库架构：

**新建文件：**
- `app/db/system_models.py` - system.db的12张ORM模型
- `app/db/system_db.py` - 系统数据库管理器
- `app/db/_module_mixins.py` - 20个Mixin基类
- `scripts/init_new_db.py` - 数据库重建脚本

**重写文件（6个模块模型）：**
- jav/fc2/uncensored/chinese/western/pornhub_models.py

**第二阶段修复（兼容性验证）：**
对比路由代码发现核心不兼容：路由用 `JavMovie.actor`（逗号文本字段）做 LIKE 查询，新模型用 movie_actors 多对多。
解决方案：MovieMixin 保留 actor/studio/series/maker 文本冗余字段（刮削同步写入）+ ORM relationship 重命名为 studio_ref/series_ref。

**修复文件：**
- `_module_mixins.py` - 加 actor/studio/series/maker 文本字段，studio/series relationship → studio_ref/series_ref
- `gfriends.py` - 硬编码旧表名(jav_actors→actors)、MODULE_TABLE_MAP修复
- `gfriends_importer.py` - 删除scraper.db依赖、统一表名、avatar_url路径修正
- `stats.py` - 硬编码旧表名修复

### 迁移统计（最终）
- **0** 个文件仍引用 `app.db.models`（scraper.db）
- **19** 个文件引用 `app.db.system_models`
- **57** 个文件引用 `app.utils.module_helper`
- 总计修改 **70+** 个文件
- 所有修改文件语法编译通过

### JAV模块 6大功能深度验证
1. **封面/NFO/预览图** ✅ 规范目录优先 → DB本地路径 → 远程下载 → 视频目录兜底
2. **播放** ✅ file_path 指向扫描时视频原始位置，数据目录与视频目录完全分离
3. **刮削存储** ✅ 封面下载到 data/movies/jav/{code}/，output_dir 正确指向 PROJECT_ROOT
4. **演员头像** ✅ JavActor.avatar_url → 本地/Gfriends，gfriends写模块库
5. **演员作品/时间线** ✅ JavMovie.actor.like() 文本模糊匹配正常
6. **查重对比** ✅ fingerprint用module+动态模型，compare用 _get_mod_cls
全部字段映射确认：actor(Text)/studio(String200)/series(String200)/maker(String100)
