# MDCX 多模块架构开发日志

> 日期：2026-07-13
> 版本：v2.4.0 (fix30) → 多模块架构阶段
> Git 提交：`def8b72` (第一阶段) → `1b85102` (第二阶段) → 第三阶段（未提交）

---

## 架构总览

将原有单一 JAV 有码系统扩展为 **5 模块架构**：

| 模块 | 标识 | 数据库 | 番号格式 | 状态 |
|------|------|--------|----------|------|
| JAV 有码 | `jav` | scraper.db (主库) | ABC-123 | 已有功能 |
| JAV 无码 | `uncensored` | uncensored.db | HEYZO-1234 / 111111-111 | 扫描器完成，API 待实现 |
| FC2 | `fc2` | fc2.db | FC2-PPV-123456 | 扫描器完成，API 待实现 |
| 国产 | `chinese` | chinese.db | 无固定番号，文件夹名=演员 | 全链路完成 |
| PORNHub | `pornhub` | pornhub.db | ph1234567890 (viewkey) | 扫描器完成，API 待实现 |

---

## 第一阶段：后端基础设施（commit: def8b72）

### 1. 模块数据库管理器 — `app/db/module_db.py`

- 每个模块使用**独立 SQLite .db 文件**（chinese.db / uncensored.db / fc2.db / pornhub.db）
- 单例模式 `ModuleDatabase.get_instance(module_name)`，全局共享引擎
- SQLite PRAGMA 优化：WAL 模式、busy_timeout=60s、foreign_keys=ON、synchronous=NORMAL、cache_size=-64MB、temp_store=MEMORY
- `init_all()` 批量初始化 4 个模块数据库
- `close_all()` 统一释放连接

### 2. 数据模型 — `app/db/`

#### ChineseMovie / ChineseActor (`chinese_models.py`)
- **ChineseMovie**: code, title, folder_name, folder_based_actors (JSON), extracted_actor, studio, cover_url, poster_url, thumb_url, trailer_url, sample_images, release_date, duration, rating, plot, genre, tag, source, source_url, file_path, file_size, fingerprint, play_count, view_status, status, created_at, updated_at, scraped_at
- **ChineseActor**: name, alias, avatar_url, source (folder/scraper), studio, movie_count
- 国产模块独有：`folder_name` + `folder_based_actors`（从文件夹名识别的演员列表）

#### Fc2Movie (`fc2_models.py`)
- FC2 独有字段：`is_mosaic`（是否有码）、`seller_id`（卖家ID）
- 番号格式：FC2-123456 / FC2PPV-123456

#### UncensoredMovie (`uncensored_models.py`)
- 独有字段：`source_platform`（来源平台索引）、`series`
- 番号格式：HEYZO-1234 / 111111-111 / RED0123

#### PornhubMovie (`pornhub_models.py`)
- 独有字段：`source_id`、`source_views`、`source_score`、`source_downloads`、`uploader`、`categories`
- 番号格式：ph1234567890abcdef（viewkey）

### 3. 模块配置模型 — `app/config/module_models.py`

```python
class ModulesConfig:
    jav: ModuleConfig           # enabled, media_dirs
    uncensored: ModuleConfig
    fc2: ModuleConfig
    chinese: ChineseModuleConfig # + actor_from_folder, folder_depth(1-5),
                                 #   folder_pattern, actor_blacklist,
                                 #   auto_create_actor, studio_names_as_folder
    pornhub: ModuleConfig
    scanner: ScannerConfig      # concurrent_limit, retry_count
```

### 4. 文件夹演员识别器 — `app/scraper/folder_actor.py`

国产模块核心功能，从文件夹名提取演员名：

- **黑名单过滤**：新建文件夹、合集、精选、unknown、未分类、tmp、temp、downloads 等
- **工作室名排除**：23 个已知工作室（麻豆传媒、天美传媒、果冻传媒、精东影业...）
- **多演员分割**：`+` / `.` / `_` / `&` / `，` / `、` / 空格
- **名称规则**：
  - 中文名：2-8 字（`[\u4e00-\u9fff]{2,8}`）
  - 英文名：首字母大写（`[A-Z][a-z]+`）
  - 混合名：中文+英文
- `extract_actor_from_folder()` / `clean_actor_name()` / `is_actor_folder()`

### 5. 扫描器基类 + 各模块扫描器 — `app/tasks/`

#### BaseScanner (`base_scanner.py`)
- 抽象基类，定义 `scan()` 接口
- `find_video_files()`: 递归查找视频文件
- `get_relative_path()`: 获取相对媒体目录的路径
- 支持扩展名：.mp4 .avi .mkv .mov .wmv .flv .webm

#### ChineseScanner (`chinese_scanner.py`)
- 文件夹深度可配置（1-5层）
- `generate_chinese_code()`: 生成唯一编码 `CN-{actors}-{hash8}`
- `extract_code_from_filename()`: 从文件名提取已知番号（MD/TM/91CM/MKY/SWAG/REAL/EDM/HKD/JVID）
- `extract_actor_from_filename()`: 从文件名提取中文名

#### Fc2Scanner (`fc2_scanner.py`)
- `extract_fc2_code()`: 支持 FC2-PPV-123456 / FC2-123456 / 纯数字6-7位 / [123456] 格式

#### UncensoredScanner (`uncensored_scanner.py`)
- `extract_uncensored_code()`: 支持 21 种无码前缀（HEYZO/1PONDO/CARIB/TOKYO-HOT/RED/PACOPACOMAMA...）
- 支持 `111111-111` 格式

#### PornhubScanner (`pornhub_scanner.py`)
- `extract_pornhub_code()`: 支持 `ph` + 10-20位十六进制 viewkey 格式

### 6. 后端 API 路由

#### 国产模块路由 — `app/api/routes/chinese_routes.py`（完整实现）
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/v1/chinese/actors` | 列出国产演员 |
| POST | `/api/v1/chinese/actors/scan-folders` | 从文件夹同步演员 |
| GET | `/api/v1/chinese/movies` | 列出国产影片（分页） |
| GET | `/api/v1/chinese/movies/{id}` | 国产影片详情 |

#### 模块管理路由 — `app/api/routes/modules.py`（完整实现）
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/v1/modules` | 列出 5 个模块及状态 |
| GET | `/api/v1/modules/{name}/stats` | 模块统计（影片/演员数） |
| POST | `/api/v1/modules/{name}/scan` | 触发模块扫描 |

#### FC2 / 无码 / PORNHub 路由 — `app/api/routes/`
- `fc2_routes.py` — **已创建，文件为空，待实现**
- `uncensored_routes.py` — **已创建，文件为空，待实现**
- `pornhub_routes.py` — **已创建，文件为空，待实现**

---

## 第二阶段：前端模块页面（commit: 1b85102）

### 1. API 层 — `src/api/`

| 文件 | 函数 | 状态 |
|------|------|------|
| `modules.js` | getModules / getModuleStats / scanModule | 完成 |
| `chinese.js` | getChineseActors / syncChineseFolderActors / getChineseMovies / getChineseMovie | 完成 |
| `fc2.js` | getFc2Movies | 骨架 |
| `uncensored.js` | getUncensoredActors / getUncensoredMovies | 骨架 |
| `pornhub.js` | getPornhubMovies | 骨架 |

### 2. Pinia Store — `src/stores/`

| 文件 | 功能 | 状态 |
|------|------|------|
| `modules.js` | loadModules / loadStats / triggerScan | 完成 |
| `chinese.js` | loadMovies / loadMovieDetail / loadActors / syncActors | 完成 |
| `fc2.js` | — | 骨架 |
| `uncensored.js` | — | 骨架 |
| `pornhub.js` | — | 骨架 |

### 3. 国产模块页面 — `src/views/chinese/`（完整实现）

#### Movies.vue — 国产影片列表
- 搜索框 + 扫描目录按钮
- 网格布局封面卡片（含工作室标签、文件夹演员标签）
- 分页组件
- 扫描对话框

#### Actors.vue — 国产演员列表
- 搜索 + 从文件夹同步按钮
- 演员卡片网格（头像、名称、作品数、来源标签）

#### MovieDetail.vue — 国产影片详情
- 封面展示
- el-descriptions 信息表（番号、文件夹、制作商、时长、评分、状态）
- 文件夹演员标签区域
- 简介展示

#### ActorDetail.vue — 国产演员详情
- 头像 + 统计
- 作品列表网格

### 4. 模块管理页面 — `src/views/modules/ModuleManager.vue`

- 5 模块卡片布局（JAV / 无码 / FC2 / 国产 / PORNHub）
- 每个模块：图标 + 名称 + 启用开关 + 媒体目录 + 统计 + 扫描按钮
- 模块图标映射：`{ jav: 'JP', uncensored: '18+', fc2: 'CD', chinese: 'CN', pornhub: 'WEB' }`

### 5. 组件 — `src/components/chinese/`

- **ActorFromFolder.vue**: 文件夹演员标签组件，closable 标签 + 空状态提示
- **FolderActorSync.vue**: 文件夹演员同步卡片，含规则说明 + 同步结果展示 + 错误折叠

### 6. 其他模块页面 — `src/views/`

- `fc2/Movies.vue` — **已创建，空文件**
- `uncensored/Movies.vue` — **已创建，空文件**
- `pornhub/Movies.vue` — **已创建，空文件**

---

## 第三阶段：未完成（未提交）

### 已完成

| 文件 | 内容 | 说明 |
|------|------|------|
| `app/db/fc2_models.py` | Fc2Movie 模型 | is_mosaic, seller_id 独有字段 |
| `app/db/uncensored_models.py` | UncensoredMovie 模型 | source_platform, series 独有字段 |
| `app/db/pornhub_models.py` | PornhubMovie 模型 | source_views, uploader, categories 独有字段 |
| `app/tasks/fc2_scanner.py` | Fc2Scanner + extract_fc2_code() | 支持 FC2-PPV/FC2/纯数字格式 |
| `app/tasks/uncensored_scanner.py` | UncensoredScanner + extract_uncensored_code() | 21 种无码前缀 |
| `app/tasks/pornhub_scanner.py` | PornhubScanner + extract_pornhub_code() | viewkey 正则匹配 |

### 待完成

| 任务 | 说明 |
|------|------|
| FC2 API 路由 | `fc2_routes.py` 为空，需实现 movies/actors CRUD |
| 无码 API 路由 | `uncensored_routes.py` 为空，需实现 movies/actors CRUD |
| PORNHub API 路由 | `pornhub_routes.py` 为空，需实现 movies/actors CRUD |
| API 路由注册 | `app/api/__init__.py` 未 import chinese_routes / modules / fc2 / uncensored / pornhub |
| 前端 Layout.vue Tab 导航 | 顶部未添加模块切换 Tab（JAV/无码/FC2/国产/PORNHub） |
| 前端路由注册 | `router/index.js` 未注册 /chinese /fc2 /uncensored /pornhub 路由 |
| FC2 前端页面 | `fc2/Movies.vue` 为空 |
| 无码前端页面 | `uncensored/Movies.vue` 为空 |
| PORNHub 前端页面 | `pornhub/Movies.vue` 为空 |
| 配置模型集成 | `app/config/models.py` 的 Config 类未引入 ModulesConfig 字段 |
| defaults.yaml | 未添加 modules 配置段 |
| 后端启动验证 | 服务器未重启验证模块加载是否正常 |

---

## 文件清单

### 后端新增/修改

```
MDCX-Server/app/
├── api/routes/
│   ├── chinese_routes.py      # 国产模块 API（完成）
│   ├── modules.py             # 模块管理 API（完成）
│   ├── fc2_routes.py          # FC2 API（空文件）
│   ├── uncensored_routes.py   # 无码 API（空文件）
│   └── pornhub_routes.py      # PORNHub API（空文件）
├── config/
│   └── module_models.py       # 模块配置 Pydantic 模型（完成）
├── db/
│   ├── module_db.py           # 模块数据库管理器（完成）
│   ├── chinese_models.py      # 国产数据模型（完成）
│   ├── fc2_models.py          # FC2 数据模型（完成）
│   ├── uncensored_models.py   # 无码数据模型（完成）
│   └── pornhub_models.py      # PORNHub 数据模型（完成）
├── scraper/
│   └── folder_actor.py        # 文件夹演员识别器（完成）
└── tasks/
    ├── base_scanner.py        # 扫描器基类（完成）
    ├── chinese_scanner.py      # 国产扫描器（完成）
    ├── fc2_scanner.py          # FC2 扫描器（完成）
    ├── uncensored_scanner.py  # 无码扫描器（完成）
    └── pornhub_scanner.py     # PORNHub 扫描器（完成）
```

### 前端新增/修改

```
MDCX-Desktop/src/
├── api/
│   ├── modules.js             # 模块管理 API（完成）
│   ├── chinese.js             # 国产 API（完成）
│   ├── fc2.js                 # FC2 API（骨架）
│   ├── uncensored.js          # 无码 API（骨架）
│   └── pornhub.js             # PORNHub API（骨架）
├── stores/
│   ├── modules.js             # 模块 Store（完成）
│   ├── chinese.js             # 国产 Store（完成）
│   ├── fc2.js                 # FC2 Store（骨架）
│   ├── uncensored.js          # 无码 Store（骨架）
│   └── pornhub.js             # PORNHub Store（骨架）
├── views/
│   ├── modules/
│   │   └── ModuleManager.vue  # 模块管理页面（完成）
│   ├── chinese/
│   │   ├── Movies.vue          # 国产影片列表（完成）
│   │   ├── Actors.vue          # 国产演员列表（完成）
│   │   ├── MovieDetail.vue     # 国产影片详情（完成）
│   │   └── ActorDetail.vue     # 国产演员详情（完成）
│   ├── fc2/
│   │   └── Movies.vue          # FC2 页面（空）
│   ├── uncensored/
│   │   └── Movies.vue          # 无码页面（空）
│   └── pornhub/
│       └── Movies.vue          # PORNHub 页面（空）
└── components/
    └── chinese/
        ├── ActorFromFolder.vue # 文件夹演员标签（完成）
        └── FolderActorSync.vue  # 文件夹同步卡片（完成）
```

---

## 下次继续的 TODO

1. **后端**：实现 fc2_routes / uncensored_routes / pornhub_routes API
2. **后端**：在 `app/api/__init__.py` 注册 chinese_routes + modules + fc2 + uncensored + pornhub
3. **后端**：在 `app/config/models.py` Config 类中添加 modules: ModulesConfig 字段
4. **后端**：在 `defaults.yaml` 添加 modules 配置段
5. **前端**：Layout.vue 顶部添加模块 Tab 导航
6. **前端**：router/index.js 注册 /chinese /fc2 /uncensored /pornhub 路由
7. **前端**：实现 fc2/uncensored/pornhub 的 Movies.vue 页面
8. **测试**：重启后端验证模块加载无报错
9. **Git**：提交第三阶段代码
